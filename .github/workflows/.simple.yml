name: test shell commands

on: push

jobs:
  run-shell-command:
     runs-on: ubuntu-latest
     steps:
       - name: echo a string
         run: ecсho "Hello World"
       - name: multiline script
         run: |
           node -v
           npm -v